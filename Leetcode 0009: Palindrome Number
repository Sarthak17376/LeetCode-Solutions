class Solution {
public:
    bool isPalindrome(int x) {
    string reversed = "";
    string temp_x = to_string(x);
    for(int i=temp_x.length()-1;i>=0;i--){
        reversed+=temp_x[i];
    }
    if(temp_x==reversed){
        return true;
    }
    else{
        return false;
    }
    }
};
